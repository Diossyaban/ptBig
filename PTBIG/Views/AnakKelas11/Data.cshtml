@model IEnumerable<PTBIG.Models.AnakKelas11>

@{
    ViewData["Title"] = "Data";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Berat)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tinggi)
            </td>
        </tr>
}
    </tbody>
</table>
